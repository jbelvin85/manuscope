# Example environment variables for the ManuScope project

# Backend Service
# PostgreSQL connection URL
# For local development outside Docker, you might use something like:
# DATABASE_URL=postgresql://user:password@localhost:5432/mydatabase
# When running with docker-compose, this is typically set within the docker-compose.yml
DATABASE_URL=postgres://postgres:password@db:5432/lamdb

# Frontend Service
# Base URL for API requests. When using Nginx proxy, this should be /api
VITE_API_BASE_URL=/api

# Secret for signing JWT tokens
JWT_SECRET=your-super-secret-key

